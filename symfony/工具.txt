phpStrome
httpie 模拟post传参数
---使用Vagrant搭建完整的开发环境
    适用于： 复杂的项目（第3方的包，数据库，redis缓存）|需要同时开发多个项目的情况下
 工具： vagrant-php-stack
       vagrant box add <name> <url>
       vagrant box list
       vagrant box remove <name>
       vagrant box repackage <name> 
       vagrant init [box-name] [box-url]
       vagrant up [vm-name] [--[no-]provision] [-h]
       vagrant destroy [vm-name]
       vagrant suspend [vm-name]
       vagrant reload [vm-name]
       vagrant resume [vm-name]
       vagrant halt [vm-name]
       vagrant status [vm-name] 
       vagrant package [vm-name] [--base name] [--output name.box][--include one,two,three] [--vagrantfile file]
       vagrant provision [vm-name]
       vagrant ssh [vm-name] [-c command] [-- extra ssh args]
       vagrant ssh-config [vm-name] [--host name]
PuPHPet.com ---定制脚本

-----------------------------
router
session
service

twig:
  -输出 : {{ }}
  -判断 : {% %}
  -注释 : {# #}
核心概念:用类的继承关系去管理页面之间的关系

html5 --- boilerplate
php app/console assets:install web --symlink --relative

注册/定义资源
{{asset('***.js')}}
{% javascripts '@目录路径'%}
<script type="text/javascript" src="{{asset_url}}">
{% endjavascripts %}

{% stylesheets '@目录路径'%}
<link rel="stylesheets" href="{{asset_url}}">
{% endstylesheets %}

coffeescript编写js源文件

doctrine 操作数据库
	entity  实体对象
	repository 对象操作集合
	proxy class 代理类
DQL
事件监听机制

数据库
设计数据库关系和主要字段
配置symfony2数据库连接
配置entity
生成get/set/repository  ----generate:doctrine:entities  ScourgenWebBundle
生成真实数据库 ---1、doctrine:database:create ，2、doctrine:schema:update --force. 











